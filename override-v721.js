(function(){
  const esc2=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
  const won=n=>(Number(n)||0).toLocaleString('ko-KR')+'원';
  function payClass(s){return s==='paid'?'pay-paid':s==='amount-mismatch'?'pay-mismatch':s==='review'?'pay-review':'pay-unpaid'}
  function payText(s){return s==='paid'?'입금완료':s==='amount-mismatch'?'금액불일치':s==='review'?'정보확인필요':'미입금'}
  function invoiceDoc(r,i){
    const c=r.customer||{}, settings=state.settings||{};
    const items=Array.isArray(r.items)?r.items:[];
    const orderNo=String(r.date||'').replaceAll('-','')+'-'+String(i+1).padStart(4,'0');
    const notice=esc2(settings.paymentNotice||'일요일 12시까지 입금 바랍니다.');
    const payerNotice=esc2(settings.payerNotice||'입금자명은 주문자명과 동일하게 부탁드립니다.');
    const address=esc2([c.postalCode,c.address,c.detailAddress].filter(Boolean).join(' ')||'주소 미등록');
    const account=esc2(settings.account||'계좌번호를 설정해 주세요');
    const rows=items.slice(0,7).map((x,n)=>`<tr><td>${n+1}</td><td>${esc2(x.item)}</td><td>${Number(x.qty)||0}</td><td>${won(x.unit)}</td><td>${won(x.amount)}</td></tr>`).join('');
    const blanks=Array.from({length:Math.max(0,7-Math.min(items.length,7))},()=>'<tr><td>&nbsp;</td><td></td><td></td><td></td><td></td></tr>').join('');
    return `<!doctype html><html lang="ko"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><style>
    *{box-sizing:border-box}html,body{margin:0;background:#ececec;font-family:Arial,'Noto Sans KR',sans-serif;color:#050505}.sheet{width:794px;min-height:1123px;margin:0 auto;background:#fff;padding:34px 30px 26px}.top{position:relative;border-bottom:3px solid #111;padding-bottom:24px}.brand{text-align:center;font-weight:900;font-size:23px}.title{text-align:center;font-size:76px;letter-spacing:24px;font-weight:1000;line-height:1.05;margin:18px 0 8px}.order{position:absolute;right:0;top:60px;border-collapse:collapse;font-size:16px}.order td{border:1px solid #111;padding:10px 16px;font-weight:700}.ctitle{display:flex;align-items:center;gap:13px;margin:15px 0 3px}.badge{background:#050505;color:#fff;padding:10px 18px;border-radius:4px;font-size:22px;font-weight:1000}.ctitle strong{font-size:28px}.thanks{margin:0 0 16px 158px;font-size:14px}.info{border:1.5px solid #111;border-radius:7px;display:grid;grid-template-columns:1fr 1.2fr;overflow:hidden}.info>div{min-height:56px;padding:13px 18px;border-bottom:1px solid #aaa;display:flex;align-items:center;font-size:16px}.info>div:nth-child(odd){border-right:1px solid #aaa}.info>div:nth-last-child(-n+2){border-bottom:0}.lab{min-width:86px;font-weight:900}.cname{font-size:21px;font-weight:1000}.items{width:100%;border-collapse:collapse;table-layout:fixed;margin-top:16px}.items th{background:#050505;color:#fff;border:1px solid #555;padding:10px;font-size:17px}.items td{border:1px solid #aaa;height:45px;padding:9px;text-align:center;font-size:15px}.items td:nth-child(2){text-align:left}.summary{display:grid;grid-template-columns:1fr 1.05fr;border:1.5px solid #111;border-top:0}.summary>div{padding:13px 20px;font-size:17px;line-height:2}.total{border-left:1px solid #111;display:flex;align-items:center;justify-content:center;gap:12px;font-weight:1000}.total strong{font-size:52px}.notice{border:1.5px solid #111;border-radius:7px;margin-top:16px;padding:14px 20px;font-size:18px;line-height:1.55}.notice b{font-size:25px;color:#d01818}.bottom{display:grid;grid-template-columns:1.2fr .8fr;gap:18px;margin-top:18px}.bankbox{border:1px solid #e2c449;background:#fff7ce;border-radius:7px;padding:13px 18px;margin-top:7px;line-height:1.5}.bankbox strong{font-size:28px}.qr{text-align:center;border-left:1px solid #aaa}.qrbox{width:108px;height:108px;margin:8px auto;border:6px double #111;display:grid;place-items:center;font-size:19px;font-weight:900}.foot{border-top:2px solid #111;margin-top:18px;padding:14px 8px 0;display:flex;justify-content:space-between;font-size:17px}.tabs{border-top:1px dashed #777;margin-top:18px;padding-top:14px;display:flex;gap:12px;align-items:center}.tab{border:1px solid #777;padding:9px 18px;border-radius:4px}.tab.on{background:#050505;color:#fff;font-weight:900}.note{margin-left:auto;border:1px solid #999;border-radius:4px;padding:8px 10px;max-width:290px;text-align:center;font-size:13px}@media print{html,body{background:#fff}.sheet{width:210mm;min-height:297mm;padding:9mm 8mm;margin:0}@page{size:A4 portrait;margin:0}}
    </style></head><body><div class="sheet"><div class="top"><div class="brand">🎁 땡라이브</div><div class="title">정산서</div><table class="order"><tr><td>주문일</td><td>${esc2(String(r.date||'').replaceAll('-','.'))}</td></tr><tr><td>주문번호</td><td>${esc2(orderNo)}</td></tr></table></div><div class="ctitle"><span class="badge">정산서 1</span><strong>${esc2(c.name||r.nick)}님 정산서</strong></div><div class="thanks">항상 저희 땡라이브를 이용해주셔서 감사합니다.</div><div class="info"><div><span class="lab">♙ 고객명</span><span class="cname">${esc2(c.name||'확인필요')}</span></div><div><span class="lab">⌖ 배송지</span>${address}</div><div><span class="lab">☎ 연락처</span><b>${esc2(c.phone||'연락처 미등록')}</b></div><div><span class="lab">▣ 메모</span>${esc2(c.memo||'부재 시 문 앞에 놔주세요.')}</div></div><table class="items"><thead><tr><th style="width:8%">NO</th><th>상품명</th><th style="width:14%">수량</th><th style="width:18%">단가</th><th style="width:18%">금액</th></tr></thead><tbody>${rows}${blanks}</tbody></table><div class="summary"><div>상품금액 합계 <b style="float:right">${won(r.subtotal)}</b><br>배송비 <b style="float:right">${won(r.fee)}</b></div><div class="total"><span>총 결제금액</span><strong>${(Number(r.total)||0).toLocaleString('ko-KR')}</strong><span>원</span></div></div><div class="notice"><b>● ${notice}</b><br>${payerNotice}</div><div class="bottom"><div><b>입금계좌</b><div class="bankbox">${esc2(settings.bank||'카카오뱅크')}<br><strong>${account}</strong><br>예금주 : ${esc2(settings.holder||'김미숙')}</div></div><div class="qr"><b>카카오채널 바로가기</b><div class="qrbox">QR</div><small>땡라이브 카카오채널에서<br>더 많은 혜택과 소식을 받아보세요!</small></div></div><div class="foot"><b>🎧 고객센터 ${esc2(settings.customerService||settings.phone||'010-2184-2344')}</b><span>감사합니다! 좋은 하루 되세요 :)</span></div><div class="tabs"><span class="tab on">정산서 1</span>${items.length>7?'<span class="tab">정산서 2</span>':''}<span class="tab">정산서 3</span><span class="note">※ 상품을 많이 구매하신 고객님은 정산서가 여러 장으로 분리되어 발행됩니다.</span></div></div></body></html>`;
  }
  window.openReceiptDetail=function(key){
    const r=receiptByKey(key); if(!r) return alert('정산서를 찾을 수 없습니다.');
    let modal=document.getElementById('receiptDetailModal');
    if(!modal){modal=document.createElement('div');modal.id='receiptDetailModal';modal.className='receipt-detail-modal';modal.innerHTML='<div class="receipt-detail-box" style="max-width:930px;background:#111"><div class="receipt-detail-actions no-print"><button class="btn warn" id="detailEditBtn">수정</button><button class="btn secondary" id="detailPrintBtn">인쇄</button><button class="btn bad" id="detailCloseBtn">닫기</button></div><iframe id="receiptFrame" style="width:100%;height:78vh;border:0;background:#fff"></iframe></div>';document.body.appendChild(modal)}
    modal.classList.add('show');
    const frame=document.getElementById('receiptFrame'); frame.srcdoc=invoiceDoc(r,indexOfReceiptKey(key));
    document.getElementById('detailEditBtn').onclick=()=>openReceiptEditByKey(key);
    document.getElementById('detailPrintBtn').onclick=()=>frame.contentWindow.print();
    document.getElementById('detailCloseBtn').onclick=()=>modal.classList.remove('show');
  };
  window.printOne=function(i){const r=getReceipts()[i];if(!r)return;const w=window.open('','_blank');w.document.open();w.document.write(invoiceDoc(r,i));w.document.close();w.onload=()=>setTimeout(()=>w.print(),250)};
  window.renderPayments=function(){
    const rs=getReceipts(); const rows=paymentFilter==='all'?rs:rs.filter(r=>r.payment.status===paymentFilter);
    const counts={all:rs.length,paid:rs.filter(r=>r.payment.status==='paid').length,unpaid:rs.filter(r=>r.payment.status==='unpaid').length,'amount-mismatch':rs.filter(r=>r.payment.status==='amount-mismatch').length,review:rs.filter(r=>r.payment.status==='review').length};
    document.querySelectorAll('#paymentFilterBar [data-filter]').forEach(b=>{const k=b.dataset.filter;b.textContent=({all:'전체',paid:'입금자',unpaid:'미입금자','amount-mismatch':'금액불일치',review:'정보확인필요'}[k])+' '+counts[k]});
    const box=$('paymentsTable'); if(!rows.length){box.innerHTML='<div class="empty">선택한 조건의 고객이 없습니다.</div>';return}
    box.innerHTML='<style>.pay-chip{display:inline-block;padding:6px 11px;border-radius:999px;font-weight:900}.pay-paid{background:#dff6e5;color:#12702d}.pay-unpaid{background:#ffe2e2;color:#b81717}.pay-mismatch{background:#fff0cf;color:#a55b00}.pay-review{background:#e9e6ff;color:#5844a8}</style><div class="scroll"><table style="min-width:1100px"><thead><tr><th>고객명</th><th>입금자</th><th>청구금액</th><th>실제 입금액</th><th>차액</th><th>대조결과</th><th>상세</th></tr></thead><tbody>'+rows.map(r=>{const p=r.payment?.payment||{},a=v717PaymentAmounts(r),s=r.payment.status;return `<tr><td><b>${esc2(r.customer?.name||r.nick)}</b><br>${esc2(r.nick)}</td><td>${esc2(p.payer||'-')}</td><td><b>${won(a.charged)}</b></td><td><b>${p.amount!=null?won(a.paid):'-'}</b></td><td style="font-weight:900;color:${a.diff===0?'#187a32':'#c62020'}">${p.amount==null?'-':(a.diff<0?'부족 ':'초과 ')+won(Math.abs(a.diff))}</td><td><span class="pay-chip ${payClass(s)}">${payText(s)}</span></td><td><button class="btn secondary" onclick="openReceiptDetail('${esc2(r.key)}')">정산서 보기</button></td></tr>`}).join('')+'</tbody></table></div>';
  };
})();
